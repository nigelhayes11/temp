name: Update Birlesik M3U

on:
  # Manuel tetikleme
  workflow_dispatch:

  # Otomatik 2 saatlik tetikleme (UTC)
  schedule:
    - cron: '0 0 * * *'  # Her gün 00:00 UTC
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Repo’yu çek
      - name: Checkout repository
        uses: actions/checkout@v3

      # Python kur
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      # Bağımlılıkları yükle
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install httpx

      # Scripti çalıştır
      - name: Run mokito.py
        run: python mokito.py

      # Git commit ve push
      - name: Commit and push mokito.m3u
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add Birlesik.m3u
          git commit -m "Update mokito M3U" || echo "No changes to commit"
          git push
